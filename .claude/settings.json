{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.command' | grep -qE 'python[23]?\\b' && printf '{\"hookSpecificOutput\":{\"hookEventName\":\"PreToolUse\",\"permissionDecision\":\"deny\",\"permissionDecisionReason\":\"Python is banned in this repo. Use Node.js instead (node -e or scripts/*.js).\"}}\\n' || true",
            "statusMessage": "Checking for banned tools..."
          }
        ]
      }
    ]
  }
}
